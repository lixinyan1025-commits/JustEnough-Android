# v1.4.1 验证记录

验证日期：2026-08-16

## 已完成的自动验证

- `testDebugUnitTest` 全部通过。
- `connectedDebugAndroidTest` 18/18 通过，覆盖 Room v7→v8 迁移、旧版迁移、四象限排序与编辑、充实度一次结算、AI回滚、小满单实例与关闭；同一套件已在 API29、API31、API34、API35、API36（Android 10、12、14、15、16）模拟器全部通过。
- Debug 与 AndroidTest APK 构建通过，Lint 0 错误。
- Release + R8 构建通过，versionCode 9、versionName 1.4.1。
- 正式签名证书 SHA-256 为 `7e2bd27b454b81db0aa6d456cc0f8d57df9e7841cbd11bb64a6e8d7fcc7c192c`，与 v1.4.0 一致，APK Signature Scheme v2 验证通过。
- 官方 v1.4.0 APK 覆盖安装到本版后成功启动，无崩溃。
- 源码、文档与 APK 中未发现本次会话公开过的 API Key 及通用 `sk-...` 密钥模式。
- 小满素材仍为单一 Lottie 矢量文件，APK 无旧精灵大图。
- API35 端到端流程：从今天页设置5分钟“稍后开始”后，到点由 AlarmManager 唤醒，响铃服务以前台服务启动，任务自动创建并计时，提醒页显示任务名称、目标、时间段、绑定应用与“关闭铃声/进入任务/稍后开始/取消本次”；点击“关闭铃声”后服务立即停止而任务继续计时。
- API35 绑定应用与锁屏流程：为“阅读”绑定 Chrome 后设置5分钟稍后开始；锁屏状态到点仍启动响铃前台服务，解锁后任务已自动开始；保持解锁的干净状态下到点会自动打开 Chrome，并启动任务说明浮层服务；“进入任务”也能打开绑定应用并显示同一浮层。无悬浮窗权限时改为通知兜底。

## 本轮功能核对

- 到点闹钟式循环铃声、关闭铃声、进入任务、稍后开始、取消本次；自动开始与自动打开绑定应用可按安排分别控制。
- 任务说明浮层、系统/自定义/内置铃声、响铃时长、音量渐强和振动。
- 小满信息面板立即收起、外部触摸透传、气泡文字对齐修正；小满提示管理移入设置。
- 四象限点击标题/空白/添加入口进入详情；编辑象限保存生效；跨区拖动保留。
- 今天页日期、秒级时钟与下一任务倒计时；稍后开始倒计时任务。

## 仍需真机认证

模拟器无法代替厂商后台策略。真我 Neo8 / realme UI 7.0 仍需完成到点响铃、锁屏提醒、后台自动打开绑定应用、任务说明浮层、小满触摸透传、待机与重启恢复的验收；其后按小米、华为、荣耀、OPPO 系、vivo 系、三星、原生 Android 的真机取得顺序加入认证名单。

## v1.4.6 验证记录（2026-08-16）

- 根因：Android 12+ 的 `TYPE_APPLICATION_OVERLAY` 悬浮窗采用 `USE_OPACITY` 触摸穿透，半透明输入框背景所在区域被系统视为透明，点击会直接落到下层应用，输入法因此无法唤起。修复为输入框使用完全不透明底色，并保留面板其余部分的透明视觉。
- 点击输入框时临时移除 `FLAG_NOT_FOCUSABLE` 让聊天窗真正获得窗口焦点，失焦后恢复，从而稳定唤起系统输入法；键盘出现时面板自动上移到屏幕顶部，避免输入行被键盘遮挡。
- API35 模拟器端到端实测：点击输入框 → `mInputShown=true` 且 IME 服务目标为悬浮窗内 `EditText` → 键入文字 → 点“发送” → 消息写入本地、面板刷新；接入本地模拟 OpenAI 兼容接口后，真实 HTTP 请求发出并收到回复，`assistant` 消息带 `PRIMARY` 来源写回并显示在悬浮窗。
- 验证输入、发送、回复完整链路后完成构建：`testDebugUnitTest`、`lintDebug`、18 项 `connectedDebugAndroidTest` 全部通过。
- `assembleRelease` 正式签名成功：`versionCode 14`、`versionName 1.4.6`，证书 SHA-256 仍为 `7e2bd27b454b81db0aa6d456cc0f8d57df9e7841cbd11bb64a6e8d7fcc7c192c`；v1.4.5 覆盖安装后正常启动。
- APK 扫描确认不含测试用接口地址、测试 Key、泄漏的 API Key 或明文流量放行配置。

## v1.4.7 验证记录（2026-08-17）

- 定位并修复“能唤起输入法但不能发送”的根因：浮窗输入框此前为多行（`setSingleLine(false)`），键盘的回车/发送键会插入换行，不会触发 `IME_ACTION_SEND`。改为单行后，键盘“发送”键直接提交。
- 发送后立即清空输入框；接口失败时在聊天流中写入“没发出去：原因”，不再静默。
- API35 模拟器实测：点击输入框 → 系统输入法服务目标为浮窗内 `EditText` → 键入文字 → 按键盘发送键 → 真实 HTTP 请求发出（本地模拟 OpenAI 兼容接口收到带计划上下文的 chat 请求）→ 用户消息、`PRIMARY` 来源回复、`PET_BUBBLE` 本地气泡均写入数据库并触发面板刷新。按钮“发送”路径同步可用。
- 浮窗聊天与 App 内「AI 分析」共用 `aiClient.chat`，模型/地址/Key/授权校验一致，无独立旁路。
- `testDebugUnitTest`、`lintDebug` 通过；正式 `assembleRelease` 成功：`versionCode 15`、`versionName 1.4.7`，证书 SHA-256 仍为 `7e2bd27b454b81db0aa6d456cc0f8d57df9e7841cbd11bb64a6e8d7fcc7c192c`；v1.4.6 覆盖安装后正常启动。
- APK 扫描确认不含测试接口地址、测试 Key、泄漏的 API Key 或明文流量放行配置。
